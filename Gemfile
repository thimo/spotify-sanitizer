source "https://rubygems.org"

# spotify-sanitizer runs on the Ruby standard library alone (net/http, json,
# socket, securerandom, digest) so it stays installable with zero gems.
# Dev-only tooling lives here.

ruby ">= 3.0"

group :development do
  gem "minitest", "~> 5.0"
  gem "rake", "~> 13.0"
end
